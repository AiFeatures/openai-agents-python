name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: Ai-road-4-You/enterprise-ci-cd/.github/workflows/release-python.yml@v1
    secrets: inherit
